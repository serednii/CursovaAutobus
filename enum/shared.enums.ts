export enum SeatStatusEnum {
  RESERVED = "reserved",
  RESERVEDEMPTY = "reservedEmpty",
  AVAILABLE = "available",
  SELECTED = "selected",
}

export enum RoleEnum {
  ADMIN = "admin",
  GUEST = "guest",
  DRIVER = "driver",
  PASSENGER = "passenger",
}

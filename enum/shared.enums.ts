export enum SeatStatusEnum {
  RESERVED = "reserved",
  AVAILABLE = "available",
  SELECTED = "selected",
}

export enum RoleEnum {
  ADMIN = "admin",
  GUEST = "guest",
  DRIVER = "driver",
  PASSENGER = "passenger",
}
